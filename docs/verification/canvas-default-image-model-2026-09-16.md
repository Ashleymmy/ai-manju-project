# 画布默认生图模型验收（2026-09-16）

## 修改

- 将画布新节点默认生图模型设为优先选择 `gpt-image-2.5-flare`。
- 仅从后台返回的模型目录中选择，匹配时忽略供应商前缀。
- 保留已选择的有效模型；不修改已有节点保存的模型。
- 指定模型未提供时，沿用原有可用默认模型的回退逻辑。

## 验证

本次修改后的 Studio 检查结果：

- `check`：退出码 0。
- `test`：126 个测试文件通过，686 项测试通过。
- `build`：退出码 0，`✓ built in 2.75s`。

正常登录本地后台后读取 `/api/ai/models`，并分别调用两个已启用供应商的模型发现接口（未更改供应商配置）。两者模型发现均成功且无 `models_error`，当前均仅返回：

```text
gpt-image-1
gpt-image-1.5
gpt-image-2
```

当前本地目录与用户截图不同，尚未提供 `gpt-image-2.5-flare`。本次没有添加虚构模型，也未发起付费生图请求；暂不能确认该模型在当前环境可调用。后台目录提供该模型后，新建画布节点会优先选择它。

## 关键帧生成页面同步

- 截图中的“关键帧生成”页面也优先选择真实目录中的 `gpt-image-2.5-flare`。
- 画布与关键帧页面共用默认模型常量，避免默认值不一致。
- 去除偏好加载时提前设置模型的分支，避免旧偏好先返回时覆盖新的默认选择。
- 用户在页面中手动切换模型仍然有效；模型未提供时沿用可用默认值。

本次同步修改后的实际验证输出（2026-09-16）：

```text
> tsc --noEmit
Exit code: 0

Test Files  126 passed (126)
     Tests  686 passed (686)
  Duration  5.72s

> pnpm run build:deps && vite build
✓ built in 2.76s
Exit code: 0
```

构建首次因包管理器工具缓存目录的写入权限失败，获准后重新执行完整构建并通过。未改动后端配置或发起生图请求。

## 画布入口完整排查

用户要求排查画布内其他图片模型默认值后，补齐以下行为：

- 图像专用目录和完整 AI 模型目录共用 `pickDefaultImageModel`，真实目录提供 flare 时统一优先选择它。使用公共目录默认值的其他前端入口也获得相同默认值。
- 单节点切换图像模型只更新该节点，不再改变新建节点、未指定模型节点的默认值。
- 新建图片节点和生图配置节点都初始化图像默认模型；连线创建、文本转图片通过相同节点创建入口。
- Agent 未指定模型的配置节点、批量及分组生图、导入图片的编辑入口均读取同一个画布默认值。节点已经指定模型时继续尊重该选择。
- 图像目录请求失败时，可使用成功返回的完整模型目录。两次请求都失败时，不使用未核实的历史偏好填充模型。
- 模型尚未就绪时不发送空模型的生图请求，避免服务端悄悄使用旧默认值。已提交任务的恢复轮询仍可正常继续。

新增回归验证使用模拟服务返回，覆盖两个模型目录一致、flare 缺失/空目录回退、明确节点选择与普通节点/Agent 批量默认值隔离、目录未就绪时不提交也不改节点。未发起付费生图请求。

本次实际检查输出：

```text
> tsc --noEmit
Exit code: 0

Test Files  126 passed (126)
     Tests  691 passed (691)
  Duration  5.82s

> pnpm run build:deps && vite build
✓ built in 2.80s
Exit code: 0
```

`git diff --check` 通过。仅修改 Studio，本次未更改后端、Worker 或 Agent 服务代码。
