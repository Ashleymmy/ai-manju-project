# 资产移动目录显示层级（2026-09-16）

## 修改

- 截图中的“移动到目录”下拉框只列出当前可见目录树的一级文件夹和直接子文件夹（二级）。
- 延续系统归档根目录、自动日期目录的隐藏规则，在整理后的目录树上计算层级。
- 同名子文件夹显示为“父文件夹 / 子文件夹”；选择框加宽并使用圆角。
- 点击侧栏的三级及以下目录时，下拉框回到未选择状态，同时禁用移动，防止向未显示的目标误移动。
- 不修改目录或素材数据，完整目录仍可通过侧栏浏览。

## 验证

Studio 实际输出：

```text
> tsc --noEmit
Exit code: 0

Test Files  126 passed (126)
     Tests  691 passed (691)
  Duration  5.76s

> pnpm run build:deps && vite build
✓ built in 2.86s
Exit code: 0
```

`git diff --check` 通过。未修改后端或执行真实资产移动。本次未进行浏览器交互验证。
